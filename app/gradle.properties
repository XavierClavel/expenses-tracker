# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Android
android.useAndroidX=true
android.nonTransitiveRClass=true

# Kotlin Multiplatform / Compose Multiplatform
kotlin.mpp.androidSourceSetLayoutVersion=2
# The iOS targets can only be built on a host with Xcode; on Linux/Windows the
# Android side of the build still configures and runs.
kotlin.native.ignoreDisabledTargets=true
org.jetbrains.compose.experimental.uikit.enabled=true

# AGP 9 ships its own built-in Kotlin support, which clashes with the Kotlin
# Multiplatform plugin on `com.android.application` modules. Opting out keeps the
# single-module layout (and therefore the CI/fastlane paths) working.
android.builtInKotlin=false
android.newDsl=false
