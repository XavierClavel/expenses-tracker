apiVersion: v1
kind: Service
metadata:
  name: expenses-tracker-nodeport
spec:
  type: NodePort
  selector:
    app: expenses-tracker-backend
  ports:
    - port: 9000
      targetPort: 8080
      nodePort: 30081
