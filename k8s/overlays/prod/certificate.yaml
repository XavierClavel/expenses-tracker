apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bankable
spec:
  secretName: bankable-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: "moneymind.fyi"
  dnsNames:
    - "moneymind.fyi"
